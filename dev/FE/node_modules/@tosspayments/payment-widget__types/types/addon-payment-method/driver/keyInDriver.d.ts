import { KeyInPaymentParams, KeyInPaymentState } from '../paymentParams/keyInPaymentParams';
interface Options {
    globalTraceId: string;
}
export declare class KeyInDriver {
    private paymentWidgetClientKey;
    private globalTraceId;
    private customerKey;
    private clientKey;
    private paymentSDK;
    private variantKey;
    constructor(paymentWidgetClientKey: string, customerKey: string, options: Options);
    setPaymentMethodWidgetVariantKey(paymentMethodWidgetVariantKey: string): void;
    initialize(): Promise<void>;
    requestPayment(state: KeyInPaymentState): Promise<void | {
        paymentType: "KEYIN";
        paymentKey: string;
        amount: number;
        orderId: string;
    }>;
    getRequestPaymentParams(state: KeyInPaymentState): KeyInPaymentParams;
    private fetchClientKey;
    getClientKey(): string;
}
export {};

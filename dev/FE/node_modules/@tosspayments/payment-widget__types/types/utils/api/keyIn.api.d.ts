export interface KeyInConfigResponse {
    clientKey: string;
}
export declare function fetchKeyInConfig({ payload, headers, }: {
    payload: {
        clientKey: string;
        variantKey: string;
    };
    headers?: Record<string, string>;
}): Promise<KeyInConfigResponse | null>;
